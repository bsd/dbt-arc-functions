---
version: 2
sources:
  - name: frakture_everyaction_deliverability
    database: "{{ var('database') }}"
    schema: src_frakture
    tables: "{{ var('sources').frakture_everyaction_deliverability.tables }}"
