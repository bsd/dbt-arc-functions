---
version: 2
sources:
  - name: frakture_everyaction_transactions
    database: "{{ var('database') }}"
    schema: src_frakture
    tables: "{{ var('sources').frakture_everyaction_transactions.tables }}"
