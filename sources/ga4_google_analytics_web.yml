version: 2
sources:
  - name: ga4_google_analytics_web
    database: "{{ var('database') }}"
    schema: "{{var('ga4_schema')}}"
    tables: 
      - name: events_%

# TODO: ga4_schema variable above needs to be incorporated into first_profiles_fixer
