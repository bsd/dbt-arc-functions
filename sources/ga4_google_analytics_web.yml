# RUN compile_sources.py to add appopriate vars in profiles.yml

version: 2
sources:
  - name: ga4_google_analytics_web
    database: "{{ var('database') }}"
    schema: "{{ var('sources').ga4_google_analytics_web.schema }}"
    tables: 
      - name: events_


