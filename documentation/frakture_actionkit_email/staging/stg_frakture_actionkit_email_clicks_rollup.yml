---
version: 2
models:
  - name: stg_frakture_actionkit_email_clicks_rollup
    description: A stage table that keeps a rollup of total clicks by message
    columns:
      - name: message_id
        description: Unique identifier for the message
      - name: clicks
        description: Number of clicks total for the message
