---
version: 2
models:
  - name: stg_web_daily_jobs_unioned
    description: This is a staging table containing unioned data for daily web jobs.
    columns:
      - name: _dbt_source_relation
        description: Metadata added by dbt. Specifies the source file and table name
          of the column.
      - name: session_date
        description: The date of the user session.
      - name: session_key
        description: Unique identifier for the user session.
      - name: default_channel_grouping
        description: The default channel grouping for the user session.
      - name: session_medium
        description: The medium of the user session.
      - name: session_source
        description: The source of the user session.
      - name: session_campaign
        description: The campaign of the user session.
      - name: hostname
        description: Hostname where the session occurred.
      - name: page_path
        description: The path of the page viewed.
      - name: referrer
        description: The referrer of the page viewed.
      - name: geo_region
        description: The geographic region where the session occurred.
      - name: device_category
        description: The category of the device used for the session.
      - name: device_language
        description: The language of the device used for the session.
