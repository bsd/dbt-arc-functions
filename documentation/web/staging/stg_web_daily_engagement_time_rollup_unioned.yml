---
version: 2
models:
  - name: stg_web_daily_engagement_time_rollup_unioned
    description: 'Model containing the unioned rollup of daily engagement time data from the web app'
    columns:
      - name: _dbt_source_relation
        description: 'Name of the source relation in the database'
      - name: session_date
        description: 'Date of the user session'
      - name: session_key
        description: 'Unique key for identifying the user session'
      - name: engagement_time_msec
        description: 'Total engagement time in milliseconds for the user session'